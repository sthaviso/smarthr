export default {
  namespace: 'overview',
  state: {
  },
  subscriptions: {
    init ({ dispatch }) {
      // dispatch({ type: 'fetchApiIntegrations' })
    },
  },
  effects: {
  },
  reducers: {
  },
}
